spring:
  config:
    activate:
      on-profile: test # 테스트 프로파일 설정
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  data:
    redis:
      host: ${SPRING_REDIS_HOST}
      port: ${SPRING_REDIS_PORT}
  jwt:
    secret: ${JWT_SECRET}